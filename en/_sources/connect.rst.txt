Connecting
==========

Access to the GH Cluster is through SSH (Secure Shell). The cluster’s address is
``gh.ccs.usherbrooke.ca``. Use your CCDB username, password, and MFA devices.

For both convenience and security, we recommend configuring an SSH public key to
connect to the cluster rather using your password. Use an SSH multiplexer to
avoid repeated MFA prompts.
